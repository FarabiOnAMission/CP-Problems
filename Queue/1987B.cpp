#include<bits/stdc++.h>

using namespace std;

int main()
{
    long long n;
    cin>>n;
    vector<long long>d;
    for(long long i=0;i<n;i++)
    {
        cin>>d[i];
    }

    long long c=INT_MIN;
    for(long long i=1;i<n;i++)
    {
        if(d[i]<d[i-1])
    }

    sort(d.begin(),d.end());

}
